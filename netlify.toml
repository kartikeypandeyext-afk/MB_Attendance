[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Every function is reachable at /.netlify/functions/<name>
# e.g. /.netlify/functions/login-by-mail
